# YYC³ 推进计划 — 基于项目实际的完整实施方案

> **日期**: 2026-04-24
> **原则**: 不建设已有功能，只接通和封装
> **核心**: 从IDE开始，本地闭环，安全第一

---

## 第一步: 验证当前状态 (30分钟)

> **目标**: 确认哪些功能已经可用，哪些需要修复
> **为什么**: 没有验证就不能推进

### 操作清单

```
请在浏览器 http://localhost:3218/ 逐项确认:

验证1: 首页 `/#/`
  □ 页面正常显示
  □ 左侧边栏可见（6个板块折叠菜单）
  □ 顶部栏Logo可见
  □ 底部栏显示

验证2: Ghost登录
  □ 首页能看到登录页面（如果已登录则跳过）
  □ 点击 "Ghost Mode" 按钮能进入

验证3: IDE页面 `/#/ide`
  □ 页面正常显示（文件树 + 代码区 + AI面板 + 终端）
  □ 左侧文件树可见
  □ 代码编辑器可见
  □ 底部终端可见
  □ 右侧AI对话面板可见

验证4: SDK Chat `/#/sdk-chat`
  □ 页面正常显示
  □ 能选择模型（Z.ai / glm-4-flash等）
  □ 输入消息后能发送
  □ 能收到AI回复

验证5: 模型配置 `/#/models`
  □ 页面正常显示
  □ 能看到9个内置服务商
  □ Z.ai显示已连接
  □ Ollama显示已连接

验证6: 酒店仪表盘 `/#/hotel-dashboard`
  □ 页面正常显示

验证7: AI Family `/#/ai-family`
  □ 页面正常显示
  □ 侧边栏展开能看到19个子页面

验证8: 系统设置 `/#/settings`
  □ 页面正常显示
```

**结果处理**:
- 全部通过 → 进入第二步
- 有失败 → 截图发给我，我定位修复

---

## 第二步: IDE → AI接通 (核心突破)

> **目标**: IDE内AIChatPanel能调用智谱API进行对话
> **原因**: 这是您说的"从IDE开始"的核心

### 现状分析

IDE中已有 `AIChatPanel.tsx`，但它目前可能是独立实现。
SDK Chat页面已有 `SDKChatPanel.tsx`，已接通智谱API。

**需要做的事**: 让IDE的AIChatPanel复用useBigModelSDK的能力

### 具体操作

```
文件1: src/app/components/ide/AIChatPanel.tsx
  当前: 独立实现（可能用mock数据）
  操作: 引入 useBigModelSDK hook
  改动:
    - import { useBigModelSDK } from "../../hooks/useBigModelSDK"
    - 用 sendMessage() 替换mock响应
    - 流式渲染接入 onChunk 回调
    - 连接状态显示 (idle/connecting/connected/error)

文件2: src/app/store/slices/ide-settings-slice.ts
  当前: IDE设置
  操作: 确认已有关联providerId的配置
  改动: 如缺少则添加默认providerId: "zhipu"
```

### 验证标准

```
打开 `/#/ide`
  □ 右侧AI面板能输入消息
  □ 发送后显示"正在思考..."
  □ 收到智谱AI的流式回复
  □ 回复中代码块有语法高亮
  □ 连接状态显示绿色"已连接"
```

---

## 第三步: Electron封装测试

> **目标**: 桌面端App可用
> **原因**: Web端文件系统受限，Electron解锁完整能力

### 操作

```bash
# 安装Electron依赖
cd /Volumes/Development/yyc3-77/YYC3-AI-PAI/YYC3-CloudIntelli-Matrix
pnpm add -D electron electron-builder electron-updater

# 启动Electron开发模式
pnpm electron:dev
```

### 验证标准

```
Electron窗口打开
  □ 标题显示 "YYC³ Cloud Intelli-Matrix"
  □ 页面正常渲染
  □ 菜单栏正常
  □ 系统托盘图标显示
```

### 需要确认的文件

| 文件 | 存在 | 功能 |
|------|------|------|
| electron/main.ts | ✅ | 主进程 |
| electron/preload.ts | ✅ | 安全桥接 |
| electron/ipc-handlers.ts | ✅ | IPC处理器 |
| electron/database-handlers.ts | ✅ | 数据库处理器 |
| electron/permission-manager.ts | ✅ | 权限管理 |
| package-electron.json | ✅ | Electron构建配置 |

---

## 第四步: 文件系统真实化

> **目标**: IDE能读写本机真实文件
> **前提**: 第三步Electron封装完成

### 操作

```
文件1: src/app/components/ide/FileExplorer.tsx
  当前: 使用mock数据 (ide-mock-data.ts)
  操作: 接入Electron IPC
  改动:
    - 检测 window.electronYyc3 是否存在
    - 存在 → 调用 fileSystem.listDirectory() 读取真实目录
    - 不存在 → 保持mock fallback

文件2: src/app/components/ide/CodePreviewPanel.tsx
  当前: mock文件内容
  操作: 接入Electron IPC读取真实文件
  改动:
    - 选中文件 → fileSystem.readFile(path)
    - 保存文件 → fileSystem.writeFile(path, content)
    - CodeMirror内容变更 → 自动保存/debounce保存

文件3: src/app/hooks/useHostFileSystem.ts
  当前: 已存在（40个hooks之一）
  操作: 确认IPC调用链完整
```

---

## 第五步: 一体化DevOps闭环

> **目标**: Matrix内集成全部运维能力

### 操作

```
IDE主面板 → 已有
  ├── 代码编辑 → CodeMirror (已有)
  ├── AI对话 → 第二步接通
  ├── 文件管理 → 第四步真实化
  ├── 终端 → xterm (已有)
  ├── Git面板 → GitPanel (已有)
  ├── 部署 → DeployDialog (已有)
  └── GPU监控 → GPUNodeCard (已有)

IDE内快速跳转（已通过路由实现）:
  `/#/ide` → IDE
  `/#/` → 数据监控
  `/#/patrol` → 巡查
  `/#/hotel-dashboard` → 酒店仪表盘
  `/#/sdk-chat` → AI对话
  `/#/models` → 模型配置
  `/#/security` → 安全监控
```

---

## 第六步: 酒店行业化

> **目标**: 酒店KPI仪表盘接入真实数据
> **前提**: 前五步完成

### 已有基础

| 组件 | 状态 | 说明 |
|------|------|------|
| HotelDashboard.tsx | ✅ 已存在 | 路由 `/hotel-dashboard` |
| ai-family-hotel-manager.ts | ✅ 已存在 | 酒店管理逻辑 |
| ai-family-hotel.types.ts | ✅ 已存在 | 酒店类型定义 |
| ai-family-hotel-scenarios.test.ts | ✅ 已存在 | 场景测试 |
| `/ai-family/hotel` 子路由 | ✅ 已存在 | 侧边栏可导航 |

### 需要做的事

```
1. HotelDashboard接入真实KPI数据源
   - RevPAR (每可用房收入)
   - OCC (入住率)
   - ADR (平均房价)

2. 接入方式选择:
   方案A: 本地CSV/JSON文件 → useHostFileSystem读取
   方案B: 本地数据库 → DatabaseAdapter查询
   方案C: API接口 → fetch从后端获取

3. 团队管理Agent
   - 使用 useAgentOrchestrator hook
   - 绑定酒店管理场景
```

---

## 第七步: DGX部署

> **目标**: NVIDIA DGX GP10 启动Ollama本地推理
> **前提**: 前六步在本机验证完成

### 操作

```
1. DGX干净环境安装
   - Docker + Ollama
   - 拉取模型: llama3, qwen2, glm4等
   - 配置Ollama端口: 11434

2. Matrix配置更新
   - .env: OLLAMA_HOST=http://dgx-ip:11434
   - provider-slice自动发现DGX上的模型

3. 验证
   - `/#/models` 页面看到DGX模型列表
   - SDK Chat能调用DGX本地模型
   - Token使用本地推理，无API费用
```

---

## 推进路线图总览

```
第一步: 验证当前状态         ← 您现在要做
  │   (30分钟, 无代码改动)
  │
第二步: IDE→AI接通           ← 核心突破
  │   (修改2个文件)
  │
第三步: Electron封装测试      ← 桌面端
  │   (安装依赖+启动)
  │
第四步: 文件系统真实化        ← 实用化
  │   (修改3个文件)
  │
第五步: 一体化DevOps闭环      ← 整合
  │   (路由已就绪, 无需新代码)
  │
第六步: 酒店行业化            ← 行业化
  │   (HotelDashboard数据接入)
  │
第七步: DGX部署              ← 最终形态
      (Ollama本地推理)
```

---

## 立即行动

**现在请您做一件事**:

打开 `http://localhost:3218/` ，按照 **第一步的验证清单** 逐项确认。

把结果告诉我：
- ✅ 正常的
- ❌ 有问题的（截图或描述）

验证结果决定第二步的具体操作。我不做任何代码修改，直到您确认第一步。

---

*本计划基于源码逐文件审核结果制定，不做假设，不建设已有功能*
